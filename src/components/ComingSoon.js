import { Typography, Card, Container } from '@mui/material';

export default function ComingSoon() {
  return (
    <Card style={{ marginTop: '25px' }}>
      <Typography style={{ textAlign: 'center', padding: '10px' }} variant='h1'>
        Coming Soon
      </Typography>
    </Card>
  );
}
